<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|Xbox">
      <Configuration>Debug</Configuration>
      <Platform>Xbox</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|Xbox">
      <Configuration>Release</Configuration>
      <Platform>Xbox</Platform>
    </ProjectConfiguration>
  </ItemGroup>
  <PropertyGroup Label="Globals">
    <VCProjectVersion>16.0</VCProjectVersion>
    <ProjectGuid>{$guid1$}</ProjectGuid>
    <Keyword>RXDK</Keyword>
    <MinimumVisualStudioVersion>15.0</MinimumVisualStudioVersion>
    <ApplicationType>RXDK</ApplicationType>
    <ApplicationTypeRevision>Current</ApplicationTypeRevision>
    <RootNamespace>XboxNamespace</RootNamespace>
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
    <ProjectName>$safeprojectname$</ProjectName>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Xbox'" Label="Configuration">
    <UseDebugLibraries>true</UseDebugLibraries>
    <PlatformToolset>RXDK</PlatformToolset>
    <ConfigurationType>DebuggerExtension</ConfigurationType>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|Xbox'" Label="Configuration">
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>RXDK</PlatformToolset>
    <ConfigurationType>DebuggerExtension</ConfigurationType>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <ImportGroup Label="ExtensionSettings" />
  <ImportGroup Label="Shared" />
  <ImportGroup Label="PropertySheets" />
  <PropertyGroup Label="UserMacros" />
  <!-- Real VC++ Linker property (Additional Dependencies): exact filenames including .lib,
       nothing appended automatically. $(D) (Debug -> "d", Release -> "") is this project's
       own explicit choice, same as picking libxbdmd.lib vs libxbdm.lib by hand. A DXT (debug
       monitor extension) has no XAPI/CRT bring-up (no libcpp, no XapiInitProcess call), so
       it never runs XAPI's DLL startup path; it still links libxapi for the heap object
       (RtlAllocateHeap/XapiProcessHeap, in libxapi/rtl/heap.c + k32/xapiheap.c) that libc's
       malloc/sprintf need on demand: see the layering note atop libc/xbox/heapalloc.c. -->
  <ItemDefinitionGroup>
    <Link>
      <LibraryDependencies>libxbdm$(D);libxapi$(D);libkernel$(D);libc$(D)</LibraryDependencies>
    </Link>
  </ItemDefinitionGroup>
  <ItemGroup>
    <ClCompile Include="src\main.c" />
    <ClInclude Include="src\defines.h" />
    <ClInclude Include="src\font.h" />
  </ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
